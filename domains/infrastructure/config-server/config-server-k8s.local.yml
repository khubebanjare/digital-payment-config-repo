# Config Server - Kubernetes Local Configuration
spring:
  cloud:
    config:
      server:
        git:
          uri: ${CONFIG_REPO_URI:file://${user.home}/config-repo}
          default-label: main
  kubernetes:
    discovery:
      enabled: true
