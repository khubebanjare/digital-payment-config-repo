  spring:
    datasource:
      url: jdbc:postgresql://postgres:5433/auth_k8s_db
      username: postgres
      password: postgres
      driver-class-name: org.postgresql.Driver

    jpa:
      hibernate:
        ddl-auto: update
      show-sql: true

    sql:
      init:
        mode: always